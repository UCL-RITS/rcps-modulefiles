#%Module -*- tcl -*-
##
## generated by ccspapp on 2015-12-18 12:10:04 +0000
## using cmd line:
##  "-p /shared/ucl/apps/zeromq/4.1.4/gnu-4.9.2 -c zeromq -o /tmp/zeromq-modules.sYbXlcxw/zeromq/4.1.4/gnu-4.9.2 -r libsodium/1.0.6/gnu-4.9.2 -r gcc-libs/4.9.2 -w Adds zeromq 4.1.4 to your environment. ZeroMQ is a distributed messaging library that supports many message-passing patterns and methods."

proc ModulesHelp { } {

  puts stderr {Adds zeromq 4.1.4 to your environment. ZeroMQ is a distributed messaging library that supports many message-passing patterns and methods.}

}

module-whatis {Adds zeromq 4.1.4 to your environment. ZeroMQ is a distributed messaging library that supports many message-passing patterns and methods.}

prereq libsodium/1.0.6/gnu-4.9.2
prereq gcc-libs/4.9.2

conflict zeromq

set              prefix               /shared/ucl/apps/zeromq/4.1.4/gnu-4.9.2


prepend-path        CMAKE_PREFIX_PATH    $prefix

if { [file isdirectory $prefix/bin] } then {
  prepend-path      PATH                 $prefix/bin
}

if { [file isdirectory $prefix/man] } then {
  prepend-path      MANPATH              $prefix/man
}

if { [file isdirectory $prefix/share/man] } then {
  prepend-path      MANPATH              $prefix/share/man
}

if { [file isdirectory $prefix/lib] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib
  prepend-path      LD_LIBRARY_PATH      $prefix/lib
}

if { [file isdirectory $prefix/lib64] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib64
  prepend-path      LD_LIBRARY_PATH      $prefix/lib64
}

if { [file isdirectory $prefix/lib/pkgconfig] } then {
  prepend-path      PKG_CONFIG_PATH      $prefix/lib/pkgconfig
}

if { [file isdirectory $prefix/include] } then {
  prepend-path      CPATH                $prefix/include
  prepend-path      INCLUDE_PATH         $prefix/include
}

