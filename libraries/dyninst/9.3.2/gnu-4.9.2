#%Module -*- tcl -*-
##
## generated by ccspapp on 2017-10-10 17:37:46 +0100
## using cmd line:
##  "-o /tmp/dyninst-modules.2TaFh27XaE/dyninst/9.3.2/gnu-4.9.2 -p /shared/ucl/apps/dyninst/9.3.2/gnu-4.9.2 -c dyninst -w Adds dyninst 9.3.2 to your environment. DynInst is a library for performing dynamic instrumentation of executables. -r gcc-libs -r elfutils -r libdwarf -r python/2.7.12 -r boost"

proc ModulesHelp { } {

  puts stderr {Adds dyninst 9.3.2 to your environment. DynInst is a library for performing dynamic instrumentation of executables.}

}

module-whatis {Adds dyninst 9.3.2 to your environment. DynInst is a library for performing dynamic instrumentation of executables.}

prereq gcc-libs
prereq elfutils
prereq libdwarf
prereq python/2.7.12
prereq boost

conflict dyninst

set              prefix               /shared/ucl/apps/dyninst/9.3.2/gnu-4.9.2


prepend-path        CMAKE_PREFIX_PATH    $prefix

if { [file isdirectory $prefix/bin] } then {
  prepend-path      PATH                 $prefix/bin
}

if { [file isdirectory $prefix/man] } then {
  prepend-path      MANPATH              $prefix/man
}

if { [file isdirectory $prefix/share/man] } then {
  prepend-path      MANPATH              $prefix/share/man
}

if { [file isdirectory $prefix/lib] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib
  prepend-path      LD_LIBRARY_PATH      $prefix/lib
}

if { [file isdirectory $prefix/lib64] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib64
  prepend-path      LD_LIBRARY_PATH      $prefix/lib64
}

if { [file isdirectory $prefix/lib/pkgconfig] } then {
  prepend-path      PKG_CONFIG_PATH      $prefix/lib/pkgconfig
}

if { [file isdirectory $prefix/include] } then {
  prepend-path      CPATH                $prefix/include
  prepend-path      INCLUDE_PATH         $prefix/include
}

