#%Module -*- tcl -*-
##
## generated by ccspapp on 2018-09-13 12:38:02 +0100
## using cmd line:
##  "-o /tmp/ga-modules.Ii8iY0yZIk/ga/5.7-8BInts/intel-2018 -c ga -r gcc-libs -r compilers/intel/2018/update3 -r mpi/intel/2018/update3/intel -e GAROOT=/shared/ucl/apps/ga/5.7-8BInts/intel-2018 -p /shared/ucl/apps/ga/5.7-8BInts/intel-2018 -w Global Arrays (GA) is a library that provides a Partitioned Global Address Space (PGAS) programming model. This version has been compiled with 8-byte integers in the Fortran code."

proc ModulesHelp { } {

  puts stderr {Global Arrays (GA) is a library that provides a Partitioned Global Address Space (PGAS) programming model. This version has been compiled with 8-byte integers in the Fortran code.}

}

module-whatis {Global Arrays (GA) is a library that provides a Partitioned Global Address Space (PGAS) programming model. This version has been compiled with 8-byte integers in the Fortran code.}

prereq gcc-libs
prereq compilers/intel/2018/update3
prereq mpi/intel/2018/update3/intel

conflict ga

set              prefix               /shared/ucl/apps/ga/5.7-8BInts/intel-2018

setenv GAROOT /shared/ucl/apps/ga/5.7-8BInts/intel-2018

prepend-path        CMAKE_PREFIX_PATH    $prefix

if { [file isdirectory $prefix/bin] } then {
  prepend-path      PATH                 $prefix/bin
}

if { [file isdirectory $prefix/man] } then {
  prepend-path      MANPATH              $prefix/man
}

if { [file isdirectory $prefix/share/man] } then {
  prepend-path      MANPATH              $prefix/share/man
}

if { [file isdirectory $prefix/lib] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib
  prepend-path      LD_LIBRARY_PATH      $prefix/lib
}

if { [file isdirectory $prefix/lib64] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib64
  prepend-path      LD_LIBRARY_PATH      $prefix/lib64
}

if { [file isdirectory $prefix/lib/pkgconfig] } then {
  prepend-path      PKG_CONFIG_PATH      $prefix/lib/pkgconfig
}

if { [file isdirectory $prefix/include] } then {
  prepend-path      CPATH                $prefix/include
  prepend-path      INCLUDE_PATH         $prefix/include
}

