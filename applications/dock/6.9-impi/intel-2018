#%Module -*- tcl -*-
##
## generated by ccspap2 on 2019-01-28 17:33:47 +0000
## using cmd line:
##  "-w The DOCK suite of programs is designed to find favorable orientations of a ligand in a receptor. This is the Intel MPI build, intended for high-performance parallel runs. -o /tmp/dock-modules.RzPzvxLZZh/dock/6.9-impi/intel-2018 -c dock -r gcc-libs -r compilers/intel/2018/update3 -r mpi/intel/2018/update3/intel -p /shared/ucl/apps/dock/6.9-impi/intel-2018"

proc ModulesHelp { } {

  puts stderr {The DOCK suite of programs is designed to find favorable orientations of a ligand in a receptor. This is the Intel MPI build, intended for high-performance parallel runs.}

}

module-whatis {The DOCK suite of programs is designed to find favorable orientations of a ligand in a receptor. This is the Intel MPI build, intended for high-performance parallel runs.}

prereq gcc-libs
prereq compilers/intel/2018/update3
prereq mpi/intel/2018/update3/intel

conflict dock

set              prefix               /shared/ucl/apps/dock/6.9-impi/intel-2018


prepend-path        CMAKE_PREFIX_PATH    $prefix

if { [file isdirectory $prefix/bin] } then {
  prepend-path      PATH                 $prefix/bin
}

if { [file isdirectory $prefix/man] } then {
  prepend-path      MANPATH              $prefix/man
}

if { [file isdirectory $prefix/share/man] } then {
  prepend-path      MANPATH              $prefix/share/man
}

if { [file isdirectory $prefix/lib] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib
  prepend-path      LD_LIBRARY_PATH      $prefix/lib
}

if { [file isdirectory $prefix/lib64] } then {
  prepend-path      LIBRARY_PATH         $prefix/lib64
  prepend-path      LD_LIBRARY_PATH      $prefix/lib64
}

if { [file isdirectory $prefix/lib/pkgconfig] } then {
  prepend-path      PKG_CONFIG_PATH      $prefix/lib/pkgconfig
}

if { [file isdirectory $prefix/include] } then {
  prepend-path      CPATH                $prefix/include
  prepend-path      INCLUDE_PATH         $prefix/include
}

